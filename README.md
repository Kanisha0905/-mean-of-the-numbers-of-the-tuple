# -mean-of-the-numbers-of-the-tuple
ch = input("Enter a character: ")

if ch.lower() in 'aeiou':
    print(ch, "is a Vowel")
else:
    print(ch, "is a Consonant")
Output:

Enter a character: d
d is a Consonant
